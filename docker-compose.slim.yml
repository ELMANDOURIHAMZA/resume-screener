version: "3.8"

services:
  api:
    build:
      context: .
      dockerfile: Dockerfile.slim
    image: resume-screener:slim
    restart: unless-stopped
    ports:
      - "8000:8000"
    volumes:
      - ./data/models:/app/data/models:ro
    environment:
      - PYTHONUNBUFFERED=1
